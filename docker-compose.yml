name: appleesports-v2

services:
  # ── PostgreSQL Database ──
  postgres:
    image: postgres:16-alpine
    container_name: appleesports-v2-postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-gamecafe_admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-GameCafe_Secure_2026!}
      POSTGRES_DB: ${DB_NAME:-gamecafe_erp}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - appleesports_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Automated Backup Container ──
  db-backup:
    image: prodrigestivill/postgres-backup-local
    container_name: appleesports-v2-db-backup
    restart: always
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_USER: ${DB_USER:-gamecafe_admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-GameCafe_Secure_2026!}
      POSTGRES_DB: ${DB_NAME:-gamecafe_erp}
      SCHEDULE: '@daily'
      BACKUP_KEEP_DAYS: 30
      BACKUP_KEEP_WEEKS: 12
      BACKUP_KEEP_MONTHS: 12
      HEALTHCHECK_PORT: 8080
    volumes:
      - db_backups:/backups
    networks:
      - appleesports_net
    depends_on:
      postgres:
        condition: service_healthy

  # ── Redis (Optional SignalR Backplane) ──
  redis:
    image: redis:7-alpine
    container_name: appleesports-v2-redis
    restart: always
    command: redis-server --requirepass ${REDIS_PASSWORD:-redispassword}
    volumes:
      - redis_data:/data
    networks:
      - appleesports_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── .NET 8 Backend API ──
  api:
    build:
      context: ./AppleEsportsErp
      dockerfile: Dockerfile
    container_name: appleesports-v2-api
    restart: always
    ports:
      - "5016:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=${DB_NAME:-gamecafe_erp};Username=${DB_USER:-gamecafe_admin};Password=${DB_PASSWORD:-GameCafe_Secure_2026!}
      - Jwt__Secret=${JWT_SECRET:-supersecretkey_min_32_chars_long!!}
      - Jwt__RefreshSecret=${JWT_REFRESH_SECRET:-supersecretkey_refresh_min_32_chars_long!!}
      - Jwt__Issuer=AppleEsportsErp
      - Jwt__Audience=AppleEsportsErpClient
      - Jwt__AccessExpiry=24h
      - Jwt__RefreshExpiry=7d
      # Uncomment below to enable Redis Backplane when scaling out
      # - Redis__ConnectionString=redis:6379,password=${REDIS_PASSWORD:-redispassword}
    networks:
      - appleesports_net
    depends_on:
      postgres:
        condition: service_healthy

  # ── React Frontend (Production Build) ──
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: appleesports-v2-client
    restart: always
    ports:
      - "5175:80"
    networks:
      - appleesports_net

  # ── React Frontend (Local Development) ──
  client-dev:
    image: node:20-alpine
    container_name: appleesports-v2-client-dev
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
      - /app/node_modules
    command: sh -c "npm install --legacy-peer-deps && npm run dev -- --host 0.0.0.0"
    networks:
      - appleesports_net

  # ── Nginx Reverse Proxy ──
  nginx:
    image: nginx:alpine
    container_name: appleesports-v2-nginx
    restart: always
    ports:
      - "8081:80"
      - "8443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/default-local.conf:/etc/nginx/conf.d/default.conf:ro
      # Let's Encrypt Volumes
      - certbot_www:/var/www/certbot
      - certbot_certs:/etc/letsencrypt
    networks:
      - appleesports_net
    depends_on:
      - api
      - client

  # ── Certbot (Let's Encrypt) ──
  certbot:
    image: certbot/certbot
    container_name: appleesports-v2-certbot
    volumes:
      - certbot_www:/var/www/certbot
      - certbot_certs:/etc/letsencrypt
    # Run twice a day and renew if expiring
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

networks:
  appleesports_net:
    driver: bridge

volumes:
  postgres_data:
  db_backups:
  redis_data:
  certbot_www:
  certbot_certs:
